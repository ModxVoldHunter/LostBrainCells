#include <iostream>

using namespace std; // not using std because its a pain and plus i don't think you want to type 'std::cout << "hello";'

class functions { // and your class, remember that
    public: // shows public so anyone coding could use it
        int project() { // the project ya know
            cout << "hello"; // sends hello

            ;
        };
        int FLStudio() {
            string DAW = "DAW Workspace";
            string MUSIC_STRING = "DAW";
        };
};