#include <iostream> // defualt
#include <fstream> // reads files 
#include <cmath> // math because half of you can't have a single attention span

int main() {
    int functionsproject(); // called the class "function" then called the int "project"

    
}